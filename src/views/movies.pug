extends index
include movieBlock

block content
  h1 #{pageTitle}
  .movies
    each item in movies
      +movieBlock({
        id:item.id,
        title:item.title,
        year:item.year,
        rating:item.rating
      })